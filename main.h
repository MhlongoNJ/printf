#ifndef MAIN_H
#define MAIN_H

#include <stdio.h>
#include <stdarg.h>
#include <string.h>
#include <unistd.h>

/* Write a function that produces output according to a format. */
/* Prototype: */ int _printf(const char *format, ...);
/* Prototype: */ int -putchar(char c)
int printf_char(va_list val)
	int printf_string(va_list val)
#endif
